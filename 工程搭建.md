## 🧱 开发环境技术栈全览

| 模块                     | 技术选型                                                     | 开发环境说明                                                 |
| :----------------------- | :----------------------------------------------------------- | :----------------------------------------------------------- |
| **前端**                 | Next.js 14 (App Router) + React 18 + Tailwind CSS            | `next dev` 提供HMR，Turbo模式加速。                          |
| **后端 API**             | Node.js + Fastify + TypeScript                               | 使用 `tsx` 或 `nodemon` 监听文件变更自动重启。               |
| **AI 编排**              | `langchain` (JS) + `@langchain/openai` + `@langchain/pinecone`（可替换为本地向量库） | 关键链构建、流式输出。                                       |
| **大语言模型**           | OpenAI API（默认）或 本地 Ollama（`llama3`/`mistral`）       | 开发期间可切换到本地模型，节省费用，无网络限制。             |
| **向量嵌入**             | OpenAI `text-embedding-3-small` 或 本地 `nomic-embed-text`（通过 Ollama 支持） | 也可用 `transformers.js` 在Node中运行 `Xenova/all-MiniLM-L6-v2`，完全离线。 |
| **向量数据库**           | **Qdrant** (Docker 本地部署)                                 | 轻量、有直观的Web UI (端口6333)，本地开发首选；也支持Pinecone Serverless。 |
| **关系型数据库**         | PostgreSQL 15 (Docker)                                       | 存储用户、会话、文档元数据等。                               |
| **缓存 / 队列后端**      | Redis 7 (Docker)                                             | 同时用于缓存向量结果、会话存储和 BullMQ 消息队列。           |
| **文件解析**             | `pdf-parse`, `mammoth`, `unstructured-js`（可调 Unstructured API） | 本地解析优先，无需外部依赖即可跑通。                         |
| **消息队列**             | BullMQ (基于 Redis) + `bull-board`（管理界面）               | Worker 异步处理文档摄入，支持任务可视化。                    |
| **对象存储（文件暂存）** | 本地文件系统（映射为共享 Volume）                            | 开发时直接写入项目下的 `uploads/` 文件夹，生产再换 S3。      |
| **包管理**               | pnpm + Turborepo                                             | Monorepo 管理多包，共享代码、配置和脚本。                    |
| **代码质量**             | TypeScript (strict), ESLint, Prettier, Husky + lint-staged   | 统一规范，提交前自动格式化。                                 |
| **环境变量**             | `.env` 文件 + Zod 验证                                       | 保证环境变量类型安全。                                       |
| **API 调试**             | 本地 Swagger (Fastify Swagger) 或 Hoppscotch / Postman       | 快速测试接口。                                               |
| **日志**                 | `pino` (Fastify 内置) + `pino-pretty`                        | 开发时结构化日志彩色输出。                                   |
| **监控/追踪**            | LangSmith（本地可禁用）或 使用 `console` + 本地文件          | 开发阶段可不引入，需要时用 LangSmith 免费层。                |

------

## 🧭 工程架构（Monorepo）

采用 **Turborepo** 组织代码，共享 `tsconfig`、`eslint` 和工具库。本地所有外部服务（DB、Redis、Qdrant）由 `docker-compose.yml` 统一启动。

text

```
my-rag-app/
├── docker-compose.yml          # 本地基础设施
├── turbo.json
├── pnpm-workspace.yaml
├── .env.example                # 模板环境变量
├── packages/
│   ├── config-eslint/
│   ├── config-ts/
│   ├── core/                   # 共享工具、类型、langchain配置
│   ├── api/                    # 主后端服务 (Fastify)
│   │   ├── src/
│   │   │   ├── routes/         # 路由定义
│   │   │   ├── chains/         # RAG 链定义
│   │   │   ├── services/       # 业务逻辑
│   │   │   ├── plugins/        # Fastify 插件
│   │   │   └── app.ts
│   │   ├── Dockerfile.dev
│   │   ├── package.json
│   │   └── tsconfig.json
│   ├── worker/                 # 文档处理 Worker
│   │   ├── src/
│   │   │   ├── processors/     # BullMQ 沙箱处理器
│   │   │   ├── lib/
│   │   │   └── main.ts
│   │   ├── package.json
│   │   └── tsconfig.json
│   └── web/                    # Next.js 前端
│       ├── app/
│       ├── components/
│       ├── hooks/
│       ├── lib/
│       ├── next.config.js
│       ├── tailwind.config.ts
│       ├── package.json
│       └── tsconfig.json
├── .husky/
└── README.md
```



### 各模块职责

- **`api` 服务**
  对外提供 RESTful 接口和 SSE 流式端点，负责 RAG 链的构建与调用。
  开发命令：`pnpm dev`（使用 `tsx watch src/app.ts` 实现热重启）。
  关键端点：
  - `POST /api/chat` – 问答（SSE 流式）
  - `POST /api/documents/upload` – 上传文档，存入 `uploads/` 并触发 Worker 任务
  - `GET /api/documents` – 列出已上传文档及处理状态
- **`worker` 服务**
  监听消息队列，执行：文件解析 → 文本分割 → 向量化 → 存入 Qdrant → 更新 PostgreSQL 状态。
  开发命令：`pnpm dev` 运行 BullMQ Worker，自动重试失败任务。
  使用 Bull Board 可在 `http://localhost:3002/admin/queues` 查看任务状态（通过 `api` 挂载 Dashboard）。
- **`web` 前端**
  Next.js 应用，提供聊天界面和文档管理。
  - 聊天区域使用 `EventSource` 或 `fetch` 读取 `/api/chat` 的 SSE 流，逐字渲染。
  - 文档上传功能直接调用 `api` 服务。
  - 本地开发代理：通过 Next.js 的 `rewrites` 将 `/api/*` 转发给后端 `api` 服务（避免 CORS 问题）。
- **`core` 包**
  抽取共享的类型定义、LangChain 基础配置（如 LLM 实例、嵌入模型选择）、工具函数，确保 `api` 和 `worker` 使用同一套逻辑。

------

## ⚙️ 本地开发环境搭建流程

### 1. 基础设施启动

根目录 `docker-compose.yml` 定义：

yaml

```
version: '3.8'
services:
  postgres:
    image: pgvector/pgvector:pg16
    env_file: .env
    ports: ["5432:5432"]
    volumes: ["./data/postgres:/var/lib/postgresql/data"]
  redis:
    image: redis:7-alpine
    ports: ["6379:6379"]
  qdrant:
    image: qdrant/qdrant
    ports: ["6333:6333", "6334:6334"]  # REST/gRPC + Web UI
    volumes: ["./data/qdrant:/qdrant/storage"]
```



启动：

bash

```
docker compose up -d
```



### 2. 环境变量配置

根目录 `.env`（`.gitignore` 排除）：

text

```
# LLM
OPENAI_API_KEY=sk-xxx
# 或者使用本地 Ollama
# LLM_PROVIDER=ollama
# OLLAMA_BASE_URL=http://localhost:11434

# 向量嵌入
EMBEDDING_PROVIDER=openai  # 或 local-ollama

# 数据库
DATABASE_URL=postgresql://rag:rag@localhost:5432/rag
REDIS_URL=redis://localhost:6379

# 向量数据库 (Qdrant)
QDRANT_URL=http://localhost:6333
```



各包通过 `dotenv` 和 `zod` 校验环境变量，防止启动时缺失。

### 3. 项目启动

bash

```
# 安装依赖 (monorepo 根目录)
pnpm install

# 同时启动所有开发服务器
pnpm dev
```



`turbo.json` 配置：

json

```
{
  "pipeline": {
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
```



各子包的 `dev` 脚本：

- `api`: `tsx watch src/app.ts`
- `worker`: `tsx watch src/main.ts`
- `web`: `next dev`

此时：

- 前端：`http://localhost:3000`
- 后端：`http://localhost:3001`（API 独立端口，前端 next 代理 `/api`）
- Bull Board：`http://localhost:3001/admin/queues`
- Qdrant Web UI：`http://localhost:6333/dashboard`

------

## 🔄 开发工作流示例

1. **调整 RAG 链**
   在 `packages/api/src/chains/rag.chain.ts` 修改 Prompt 或检索参数，保存后 API 自动重启，前端聊天窗口刷新即可测试。
2. **上传新文档**
   通过前端界面或 `curl` 上传 PDF，`api` 接收后存储到 `./uploads`，并向 Redis 队列发送任务，Worker 自动处理。可在 Bull Board 观察任务进度，处理成功后向量库即时生效。
3. **切换本地模型**
   安装 [Ollama](https://ollama.ai/) 并拉取模型：`ollama pull llama3` 和 `ollama pull nomic-embed-text`。
   在 `.env` 中设置 `LLM_PROVIDER=ollama` 和 `EMBEDDING_PROVIDER=local-ollama`，重启即可完全离线开发，零 API 费用。
4. **调试与追踪**
   如需观察 LangChain 调用链路，可在 `core` 包中设置 `enable tracing` 并配置 `LANGSMITH_API_KEY`（免费层），所有链调用自动记录到 LangSmith。

------

## 💡 开发体验优化建议

- **统一 API 代理**：前端 `next.config.js` 配置 `async rewrites()` 将所有 `/api/*` 请求代理到 `http://localhost:3001`，避免跨域和 cookie 问题。
- **环境变量自动校验**：使用 `@t3-oss/env-core` 或 `zod` 定义 schema，启动时校验，提示缺失的变量。
- **Git 钩子**：提交前自动运行 ESLint 和 Prettier，保持代码风格一致。
- **临时跳过 LLM**：开发 UI 时，可 mock SSE 响应，返回模拟 Token 流，加速界面迭代。