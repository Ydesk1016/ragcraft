FROM node:20-alpine
WORKDIR /app
RUN corepack enable
COPY package.json pnpm-lock.yaml* pnpm-workspace.yaml turbo.json ./
COPY packages ./packages
RUN pnpm install --frozen-lockfile
CMD ["pnpm", "--filter", "@rag/api", "dev"]
